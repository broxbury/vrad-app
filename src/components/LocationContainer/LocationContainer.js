import React from 'react';
import LocationCard from '../LocationCard.js';

const LocationContainer = ({ cards }) => {
  // const locationCards = card.map yadda yadda yadda

  return(
    <div className='location-container'>
      {locationCards}
    </div>
  )
}
